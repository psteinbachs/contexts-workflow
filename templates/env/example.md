# dev

Development environment. No critical directive.

## Boundaries

- **Allowed**: localhost, development servers
- **Forbidden**: production systems

## MCP Router

- URL: http://localhost:8100
- Token: `~/.claude/env/dev.session-token`

## More Context

Use `rs` to search session history or `GET /context?environment=dev` for stored facts.
